// Define an array of fruits
var fruits = [
    "Apple",
    "Banana",
    "Orange"
];
// Attempt to access an element at an invalid index
console.log(fruits[3]); // This line will produce an error because index 3 does not exist
// Correct the error by accessing a valid index
console.log(fruits[1]); // Accessing the second element (index 1) which is "Banana"
